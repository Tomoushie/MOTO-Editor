<Project Sdk="Microsoft.NET.Sdk">

  <!-- ══════════════ Ciblage : Windows par défaut, macOS si build sur macOS ══════════════ -->
  <PropertyGroup>
    <TargetFrameworks>net8.0-windows10.0.19041.0</TargetFrameworks>
    <TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('osx'))">$(TargetFrameworks);net8.0-maccatalyst</TargetFrameworks>

    <OutputType>Exe</OutputType>
    <RootNamespace>Moto.Editor</RootNamespace>
    <UseMaui>true</UseMaui>
    <SingleProject>true</SingleProject>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- ══ Métadonnées application + identité MSIX (fusionnées, une seule fois) ══ -->
    <ApplicationTitle>MOTO Editor</ApplicationTitle>
    <ApplicationId>com.moto.editor</ApplicationId>
    <ApplicationDisplayVersion>1.0.0</ApplicationDisplayVersion>
    <ApplicationVersion>1</ApplicationVersion>
    <PublisherDisplayName>MOTO</PublisherDisplayName>
    <PackageDescription>MOTO Editor — éditeur IA 100 % local</PackageDescription>

    <!-- ══ Plateformes supportées ══ -->
    <SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.17763.0</SupportedOSPlatformVersion>
    <SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">13.1</SupportedOSPlatformVersion>

    <ApplicationIcon>Platforms\Windows\appicon.ico</ApplicationIcon>
  </PropertyGroup>

  <!-- ══════════════ Ressources MAUI ══════════════ -->
  <ItemGroup>
    <MauiIcon Include="Resources\AppIcon\appicon.png" BaseSize="512,512" />
    <MauiSplashScreen Include="Resources\Splash\splash.png" Color="#0B1526" BaseSize="256,256" />
    <MauiImage Include="Resources\Images\moto_logo.png" />
    <MauiImage Include="Resources\Images\moto_logo_128.png" />
    <MauiImage Include="Resources\Images\moto_logo_32.png" />
    <MauiFont Include="Resources\Fonts\*" />
    <MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
  </ItemGroup>

  <!-- ══════════════ .ico copié en sortie (Windows uniquement) ══════════════ -->
  <ItemGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">
    <Content Include="Platforms\Windows\appicon.ico" CopyToOutputDirectory="PreserveNewest" ExcludeFromSingleFile="true" />
  </ItemGroup>

  <!-- ══════════════ Packages NuGet ══════════════ -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Maui.Controls" Version="$(MauiVersion)" />
    <PackageReference Include="Microsoft.Maui.Controls.Compatibility" Version="$(MauiVersion)" />
    <PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="8.0.0" />
  </ItemGroup>

  <!-- ══════════════ Références projets ══════════════ -->
  <ItemGroup>
    <ProjectReference Include="..\Moto.Core\Moto.Core.csproj" />
    <ProjectReference Include="..\Snake2000.Engine\Snake2000.Engine.csproj" />
  </ItemGroup>

</Project>
