<UserControl x:Class="Moto.Editor.Views.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PanelBackgroundBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
            <TextBlock Text="🛡️ Admin Dashboard" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
            <Button Content="🔄 Actualiser" Click="OnRefreshClicked" Padding="8,4"/>
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Colonne 1 : Signalements -->
            <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="🚩 Signalements en attente" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListView x:Name="ReportsList" MaxHeight="400" Background="Transparent" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding PluginId}" FontSize="12"/>
                                    <Button Grid.Column="1" Content="✅ Approuver" Click="OnApproveClicked" Tag="{Binding Id}" Padding="4,2" FontSize="10"/>
                                    <Button Grid.Column="2" Content="🚫 Bannir" Click="OnBanClicked" Tag="{Binding Id}" Padding="4,2" FontSize="10" Background="{StaticResource ErrorBrush}" Foreground="White"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Colonne 2 : Remboursements -->
            <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="💸 Demandes de remboursement" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListView x:Name="RefundsList" MaxHeight="400" Background="Transparent" BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding UserId}" FontSize="12" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Amount, StringFormat='{}{0:C}'}" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Rembourser" Click="OnRefundClicked" Tag="{Binding Id}" Padding="6,4" Background="{StaticResource WarningBrush}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
