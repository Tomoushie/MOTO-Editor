<?xml version="1.0" encoding="utf-8" ?>
<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Moto.Editor.Views.PerformanceDashboardView">
    <Grid Background="{StaticResource PanelBackgroundBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="📊 Performance Dashboard"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryTextBrush}"/>
            <Button Content="🔄 Refresh" Click="OnRefreshClicked"
                    Padding="8,4" FontSize="12"/>
            <Button Content="📥 Export JSON" Click="OnExportClicked"
                    Padding="8,4" FontSize="12"/>
        </StackPanel>

        <!-- Métriques clés -->
        <Grid Grid.Row="1" Margin="0,16,0,16" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="RAM" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock x:Name="RamValue" Text="-- MB" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="CPU" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock x:Name="CpuValue" Text="-- %" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="Threads" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock x:Name="ThreadsValue" Text="--" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="GC Gen2" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock x:Name="GcValue" Text="--" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Liste des métriques détaillées -->
        <ListView Grid.Row="2" x:Name="MetricsList" Background="Transparent" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,6" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Name}"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Value, StringFormat='{}{0:F2}'}"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Grid.Column="2" Text="{Binding Unit}"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   FontSize="11"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer : état du profiler -->
        <TextBlock Grid.Row="3" x:Name="StatusText"
                   Text="Profiler actif · échantillonnage 2s"
                   FontSize="11"
                   Foreground="{StaticResource SecondaryTextBrush}"
                   Margin="0,8,0,0"/>
    </Grid>
</UserControl>
