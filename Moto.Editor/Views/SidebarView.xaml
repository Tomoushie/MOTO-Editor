<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Moto.Editor.Views.SidebarView"
             WidthRequest="240" BackgroundColor="{StaticResource BgSide}">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="10,8">

        <!-- Header : titre + recherche + notif -->
        <HorizontalStackLayout Grid.Row="0" Spacing="{StaticResource SpaceSm}">
            <Label Text="MOTO AI" FontAttributes="Bold" FontSize="{StaticResource FontSizeSubheading}" VerticalOptions="Center" />
            <Label Text="🔍" VerticalOptions="Center" Style="{StaticResource TxtSecondary}" />
            <Label Text="🔔" VerticalOptions="Center" Style="{StaticResource TxtSecondary}" />
        </HorizontalStackLayout>

        <!-- Nouveau chat -->
        <Border Grid.Row="1" Stroke="{StaticResource BorderCol}" StrokeShape="RoundRectangle 8"
                BackgroundColor="{StaticResource BgPanel}" Padding="10,6" Margin="0,8">
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnNewChat" />
            </Border.GestureRecognizers>
            <Label Text="＋ Nouveau chat" FontSize="{StaticResource FontSizeBody}" />
        </Border>

        <!-- Sections : Épinglés / Projets / Récents -->
        <ScrollView Grid.Row="2">
            <VerticalStackLayout Spacing="{StaticResource SpaceXs}">
                <Label Text="ÉPINGLÉS" Style="{StaticResource TxtSecondary}" Margin="0,6,0,2" />
                <VerticalStackLayout x:Name="PinnedList" Spacing="{StaticResource SpaceXs}" />

                <Label Text="PROJETS" Style="{StaticResource TxtSecondary}" Margin="0,10,0,2" />
                <VerticalStackLayout x:Name="ProjectsList" Spacing="{StaticResource SpaceXs}" />

                <Label Text="RÉCENTS" Style="{StaticResource TxtSecondary}" Margin="0,10,0,2" />
                <VerticalStackLayout x:Name="RecentsList" Spacing="{StaticResource SpaceXs}" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- Footer utilisateur -->
        <HorizontalStackLayout Grid.Row="3" Spacing="{StaticResource SpaceSm}" Padding="0,8,0,0">
            <Label Text="👤" />
            <Label Text="Tom Nowak" FontSize="{StaticResource FontSizeBody}" VerticalOptions="Center" />
        </HorizontalStackLayout>

    </Grid>

</ContentView>
