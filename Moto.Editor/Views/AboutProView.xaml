<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Moto.Editor.Controls"
             x:Class="Moto.Editor.Views.AboutProView">

    <Border Background="{DynamicResource MotoCardBackground}"
            Stroke="{DynamicResource MotoCardBorder}" StrokeThickness="1" Padding="24">
        <Border.StrokeShape><RoundRectangle CornerRadius="16" /></Border.StrokeShape>

        <ScrollView>
            <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                <controls:MotoLogoView Size="96" HorizontalOptions="Center" />
                <Label Text="MOTO Editor Pro" FontSize="26" FontAttributes="Bold" HorizontalOptions="Center" />
                <Label x:Name="VersionLabel" Text="Version —" FontSize="12" Opacity="0.7" HorizontalOptions="Center" />

                <!-- Badges Pro -->
                <FlexLayout Direction="Row" Wrap="Wrap" JustifyContent="Center">
                    <Border Padding="10,5" Margin="4" Background="{DynamicResource MotoSubtleBackground}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14" /></Border.StrokeShape>
                        <Label Text="⭐ Pro" FontSize="12" />
                    </Border>
                    <Border Padding="10,5" Margin="4" Background="{DynamicResource MotoSubtleBackground}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14" /></Border.StrokeShape>
                        <Label Text="🤖 IA locale" FontSize="12" />
                    </Border>
                    <Border Padding="10,5" Margin="4" Background="{DynamicResource MotoSubtleBackground}">
                        <Border.StrokeShape><RoundRectangle CornerRadius="14" /></Border.StrokeShape>
                        <Label Text="🧠 XENO-SSS∞" FontSize="12" />
                    </Border>
                </FlexLayout>

                <!-- Modules IA -->
                <Border Padding="14" WidthRequest="440"
                        Background="{DynamicResource MotoSubtleBackground}"
                        Stroke="{DynamicResource MotoCardBorder}" StrokeThickness="1">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12" /></Border.StrokeShape>
                    <VerticalStackLayout Spacing="6">
                        <Label Text="🧩 Modules IA actifs" FontSize="14" FontAttributes="Bold" />
                        <CollectionView x:Name="ModulesList" HeightRequest="180">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding}" FontSize="12" Padding="0,2" />
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

                <!-- Stats -->
                <Border Padding="14" WidthRequest="440"
                        Background="{DynamicResource MotoSubtleBackground}"
                        Stroke="{DynamicResource MotoCardBorder}" StrokeThickness="1">
                    <Border.StrokeShape><RoundRectangle CornerRadius="12" /></Border.StrokeShape>
                    <VerticalStackLayout Spacing="4">
                        <Label Text="📊 Statistiques" FontSize="14" FontAttributes="Bold" />
                        <Label x:Name="StatsLabel" FontSize="12" />
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </Border>
</ContentView>
